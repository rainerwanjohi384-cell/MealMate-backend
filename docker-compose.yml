version: '3.8'

services:
  backend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: mealmate-backend
    ports:
      - "8000:8000"
    environment:
      - DATABASE_URL=sqlite:///./data/mealmate.db
      - SECRET_KEY=${SECRET_KEY:-your-secret-key-change-in-production}
      - ALGORITHM=HS256
      - ACCESS_TOKEN_EXPIRE_MINUTES=30
      - ALLOWED_ORIGINS=http://localhost:3000,http://localhost:5173
    volumes:
      - ./data:/app/data
      - ./.env:/app/.env
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 5s

  # Optional: PostgreSQL database (comment out backend's DATABASE_URL if using this)
  # postgres:
  #   image: postgres:15-alpine
  #   container_name: mealmate-postgres
  #   environment:
  #     - POSTGRES_USER=mealmate
  #     - POSTGRES_PASSWORD=mealmate_password
  #     - POSTGRES_DB=mealmate
  #   volumes:
  #     - postgres_data:/var/lib/postgresql/data
  #   ports:
  #     - "5432:5432"
  #   restart: unless-stopped

# volumes:
#   postgres_data:
